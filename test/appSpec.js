define(['app'], function (userInterface) {
    describe('UserContentController', function () {

    //     var $controller, $httpBackend, $scope, users;

    //     beforeEach(module('userInterface'));

    //     beforeEach(inject(function (_$controller_, _$rootScope_, _users_, ) {
    //         $controller = _$controller_;
    //         users = _users_;
    //         $httpBackend = _$httpBackend_;
    //         $scope = _$rootScope_.$new();
    //     }));

    //     afterEach(function () {
    //         $httpBackend.verifyNoOutstandingRequest();
    //         $httpBackend.verifyNoOutstandingExpectation();
    //     });

    //     describe('$scope.userData', function () {
    //         it('loads the users list and updates the table data', function () {
    //             $httpBackend.whenGET('http://localhost:3000/users').respond({ id: 123 });
    //             users.get(123).then(function (response) {
    //                 users = resopnse;
    //             })
    //             $httpBackend.flush();
    //             expect(users).toBe(true);
    //         });
    //     })

    // })
    describe('test', function () {
        it('should pass', function () {
            expect(true).toBe(true);
        })
    })
})
})